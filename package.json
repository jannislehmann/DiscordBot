{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && NODE_ENV=development node build/main.js",
    "lint": "tslint --project '.'",
    "start": "npm run build && NODE_ENV=production node build/main.js",
    "test": "npm run lint"
  },
  "license": "MIT",
  "dependencies": {
    "discord.js": "^11.4.2",
    "mongoose": "^5.4.2"
  },
  "devDependencies": {
    "@types/mongoose": "^5.3.6",
    "@types/node": "^10.12.18",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}
